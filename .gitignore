# Test files (binaries, debug outputs, verification scripts)
test/

# Build artifacts
*.o
*.dSYM/
fe-loader.js

# WASM build output (regenerated by build.sh)
fastenhancer-worklet.js

# PyTorch checkpoints (only exported .bin is tracked)
weights/fastenhancer_*/
